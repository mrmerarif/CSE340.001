<%- include("partials/header") %>

    <main class="page page-categories">
        <h1>
            <%= title %>
        </h1>

        <section class="organizations-list">
            <h2 class="section-title">Service Project Categories</h2>

            <ul>
                <% categories.forEach(category=> { %>
                    <li>
                        <%= category.name %>
                    </li>
                    <% }) %>
            </ul>
        </section>
    </main>

    <%- include("partials/footer") %>